# 10Tap Advanced Setup Example

Advanced setup example with simple CounterBridge that bridges `@tiptap/extension-character-count`

# To Run

1. Run `yarn`
2. Build the editor `yarn editor:build`
3. Run the app `yarn ios`
